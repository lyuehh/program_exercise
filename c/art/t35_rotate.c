#include <stdio.h>
#include <string.h>

#define MAX 1000
char a[MAX][MAX];

int main(int argc, const char *argv[])
{
    char c;
    int row;
    while (scanf("%c", &c) != EOF) {
    }
    printf("row: %d\n", row);
    return 0;
}
