#include <stdio.h>
#define MAX 1000

int main(int argc, const char *argv[])
{
    int n;
    int i;
    int m[MAX];
    int count;
    int j;
    scanf("%d", &n);
    for (j=0;i<=n;j++) {
        scanf("%d", &m[j]);
    }
    for (j=0;i<=n;j++) {
        printf("%d", m[j]);
    }
    return 0;
}
